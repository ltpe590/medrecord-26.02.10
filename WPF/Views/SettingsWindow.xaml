<Window x:Class="WPF.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:WPF.Views"
        mc:Ignorable="d"
        Title="Settings"
        Height="680" Width="820"
        MinHeight="600" MinWidth="760"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="56"/>
        </Grid.RowDefinitions>

        <!-- All settings panels and tab bar live in the shared UserControl -->
        <views:SettingsContentControl x:Name="SettingsContent" Grid.Row="0"/>

        <!-- Footer: Save / Cancel — dialog-specific, not in the shared control -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Margin="20,0">
                <Button Content="Cancel" Click="CancelButton_Click"
                        Width="90" Height="36" Margin="0,0,10,0"
                        Background="#9E9E9E" Foreground="White"
                        BorderThickness="0" Cursor="Hand" FontSize="13"/>
                <Button Content="Save" Click="SaveButton_Click"
                        Width="90" Height="36"
                        Background="#2196F3" Foreground="White"
                        BorderThickness="0" Cursor="Hand" FontSize="13" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
